[supervisord]
nodaemon=true
pidfile=/var/run/supervisord.pid
logfile=/var/log/supervisor/supervisord.log

[program:sshd]
command=service sshd start

#[program:hive-metastore]
#command=service hive-metastore start
